spring:
  application:
    name: oauth2-server
server:
  port: 2020
application:
  init:
    users[0]:
      username: admin
      password: '{bcrypt}$2a$10$tMSDLeQjFehV8Z1BxoU3quISNyLXXMDI8pdA3RKdwqv1ujuZ6AAQG'
      authorities: admin
      roles: admin,test-admin
    users[1]:
      username: test-admin
      password: '{bcrypt}$2a$10$tMSDLeQjFehV8Z1BxoU3quISNyLXXMDI8pdA3RKdwqv1ujuZ6AAQG'
      authorities: admin
      roles: test-admin

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
    health-check-url-path: /actuator/health
    prefer-ip-address: true #跨域访问
  client:
    registryFetchIntervalSeconds: 5
    registerWithEureka: false # 此应用为注册中心，false：不向注册中心注册自己。
    fetchRegistry: false # 注册中心职责是维护服务实例，false：不检索服务。
    serviceUrl:
      defaultZone: http://admin:admin123@jr-server:2001/eureka/,http://admin:admin123@jr-server:2002/eureka/,http://admin:admin123@jr-server/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
logging.level.org.springframework.security: info

---
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:master}
  application:
    name: "@project.name@"
...

---
spring:
  profiles: master
  cloud:
    config:
      uri: ${configserverurl:http://localhost:2010}
      username: admin
      password: ${SECURITY_USER_PASSWORD}
...

---
spring:
  cloud:
    config:
      profile: ${PROFILE:mac}
      label: ${spring.profiles.active}
      name: "@project.name@"
      failFast: true
      retry:
        max-attempts: 10
        initial-interval: 1000
...