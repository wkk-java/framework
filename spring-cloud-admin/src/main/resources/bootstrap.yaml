spring:
  application:
    name: spring-cloud-admin
  cloud:
    nacos:
      discovery:
        server-addr: 106.14.202.235:8848
        metadata:
          user:
            name: admin
            password: ${SECURITY_USER_PASSWORD}
  profiles:
    active:
      - secure
server:
  port: 3001

logging:
  file: /Users/vince/Documents/workspace/logs/admin.log

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
---
spring:
  profiles: insecure
...

---
spring:
  profiles: secure
  security:
    user:
      name: admin
      password: ${SECURITY_USER_PASSWORD}
...